# vidcodec-mediacodec

Android MediaCodec NDK backend for [vidcodec](..).

Provides H.264 hardware encode and decode via the Android `libmediandk` API
(`AMediaCodec`).  Only supported on `target_os = "android"`.
